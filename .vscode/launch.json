
{
    "version": "0.2.0",
    "configurations": [],
    "includePath": [
        "${workspaceFolder}/**",
        "/usr/include/gtk-3.0", // Add the include path for the GTK+ library
        "/usr/include/qt5/QtWidgets" // Add the include path for the QApplication library
    ]
}